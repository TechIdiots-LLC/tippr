description "update the vault name search cache"

manual
task
stop on tippr-stop or runlevel [016]

nice 10

script
    . /etc/default/tippr
    wrap-job paster run $TIPPR_INI -c 'from r2.lib import vault_search; vault_search.load_all_reddits()'
end script
